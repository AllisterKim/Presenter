<Window x:Class="Presenter.MagnifierWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="돋보기"
        Width="240" Height="120"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="#E6262626"
            CornerRadius="14"
            BorderBrush="#33FFFFFF"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.4" BlurRadius="24" ShadowDepth="6" Direction="270"/>
        </Border.Effect>

        <Grid Margin="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button x:Name="CloseButton"
                    Content="✕"
                    Click="CloseButton_Click"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Width="22" Height="22"
                    Padding="0"
                    Foreground="#AAAAAA"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    FontSize="12"/>

            <TextBlock Grid.Row="1"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Foreground="#CCCCCC"
                       FontSize="12.5"
                       Text="화면을 드래그해 확대할 영역을 선택하세요.&#x0a;마우스 휠로 확대율을 조정할 수 있어요 (최대 3배).&#x0a;ESC: 원래대로   닫기: 돋보기 종료"/>
        </Grid>
    </Border>
</Window>
