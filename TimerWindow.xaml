<Window x:Class="Presenter.TimerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="타이머"
        Width="227" Height="171"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="#E6262626"
            CornerRadius="14"
            BorderBrush="#33FFFFFF"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.4" BlurRadius="24" ShadowDepth="6" Direction="270"/>
        </Border.Effect>

        <Grid Margin="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Slider x:Name="OpacitySlider"
                        Grid.Column="0"
                        Minimum="0.25" Maximum="1"
                        Value="1"
                        SmallChange="0.05"
                        LargeChange="0.1"
                        VerticalAlignment="Center"
                        Margin="0,0,10,0"
                        ValueChanged="OpacitySlider_ValueChanged"
                        AutoToolTipPlacement="TopLeft"
                        ToolTip="타이머 투명도"/>

                <Button x:Name="CloseButton"
                        Grid.Column="1"
                        Content="✕"
                        Click="CloseButton_Click"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Width="22" Height="22"
                        Padding="0"
                        Foreground="#AAAAAA"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        FontSize="12"/>
            </Grid>

            <!-- 분 입력 화면 -->
            <StackPanel x:Name="InputPanel"
                        Grid.Row="2"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <TextBlock Text="발표 시간(분)"
                           Foreground="#CCCCCC"
                           FontSize="12"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBox x:Name="MinutesTextBox"
                         Width="70"
                         FontSize="20"
                         TextAlignment="Center"
                         Text="5"
                         HorizontalAlignment="Center"/>
                <Button x:Name="StartButton"
                        Content="시작"
                        Click="StartButton_Click"
                        Margin="0,12,0,0"
                        Padding="20,6"
                        HorizontalAlignment="Center"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#0A84FF" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>

            <!-- 카운트다운 화면 -->
            <StackPanel x:Name="CountdownPanel"
                        Grid.Row="2"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Visibility="Collapsed">
                <TextBlock x:Name="CountdownText"
                           Text="05:00"
                           FontSize="34"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <Button x:Name="StopButton"
                        Content="정지"
                        Click="StopButton_Click"
                        Margin="0,10,0,0"
                        Padding="16,4"
                        HorizontalAlignment="Center"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#555555" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
